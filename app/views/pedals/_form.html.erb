<%= form_for(@pedal) do |f| %>
  <% if @pedal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pedal.errors.count, "error") %> prohibited this pedal from being saved:</h2>

      <ul>
      <% @pedal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :make %><br>
    <%= f.text_field :make %>
  </div>
  <div class="field">
    <%= f.label :model %><br>
    <%= f.text_field :model %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
